<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/slider_item_icon"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/work_icon_512"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/slider_item_title"
     />

    <TextView
        android:id="@+id/slider_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:padding="5dp"
        android:text="Business account"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:textColor="@color/black"
        android:textSize="35sp" />

    <TextView
        android:id="@+id/slider_item_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/slider_item_title"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:paddingHorizontal="20dp"
        android:text="You can sign up as a dog walker to offer your service to all the accounts of the app"
        android:textSize="20sp" />

</RelativeLayout>